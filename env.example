# Application settings are now hardcoded in config.py

# API settings are now hardcoded in config.py

############################################ Database Configuration ######################################
DB_HOST=localhost
DB_PORT=5435
DB_NAME=projectDB
DB_USER=scholar
DB_PASSWORD=
# Database pool settings are now hardcoded in config.py

######################################## RabbitMQ Configuration ###########################################
RABBITMQ_USER=scholar
RABBITMQ_PASSWORD=
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
# RabbitMQ settings are now hardcoded in config.py

# Queue configuration is now hardcoded in config.py

####################################### External Services #########################################
GROBID_URL=http://localhost:8070
# GROBID timeout is now hardcoded in config.py

############################################## Gemini ########################################
GA_GEMINI_API_KEY=
GA_GEMINI_MODEL=gemini-2.0-flash-exp
GA_GEMINI_RATE_LIMIT=15

# Search service URLs are not used in the code and have been removed
# Search settings are now hardcoded in config.py

######################################## Gap Analysis Configuration ###########################################
# Gap analysis settings are now hardcoded in config.py

######################################## Logging Configuration ###########################################
LOG_LEVEL=INFO
LOG_FILE=logs/gap_analysis.log
# Log settings are now hardcoded in config.py

######################################## Performance Configuration ###########################################
# Performance settings are now hardcoded in config.py

######################################## Cache Configuration ###########################################
# Cache settings are now hardcoded in config.py
